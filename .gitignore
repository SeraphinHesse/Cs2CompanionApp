# Build output
bin/
obj/
*.user
*.suo
.vs/
.idea/

# Decompiled game reference tree — large, regenerable, never committed
refsrc/

# Modding toolchain scratch. ModPostProcessor drops Library/ilpp.pid next to the project, and
# Mod.props explicitly excludes both of these from the compile — so they are its artifacts, not ours.
Library/
Logs/

# UI
node_modules/
ui/dist/
ui/.parcel-cache/

# Local runtime artifacts
*.log

# Agent worktrees - transient, never part of the repo
.claude/worktrees/
